```
sudo apt update -y && sudo apt upgrade

sudo apt install zathura wezterm

sudo snap install telegram-desktop spotify discord brave

pip install numpy pandas notebook

git clone https://github.com/igormp/imp.git "$ZSH_CUSTOM/themes/imp"
ln -s "$ZSH_CUSTOM/themes/imp/imp.zsh-theme" "$ZSH_CUSTOM/themes/imp.zsh-theme"

curl https://sh.rustup.rs -sSf | sh

cargo install ytop
```
